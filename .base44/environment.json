{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "VITE_BASE44_APP_ID",
      "description": "The Base44 app ID for this project (found in your Base44 app settings/dashboard).",
      "requiredAtBoot": false
    },
    {
      "name": "VITE_BASE44_BACKEND_URL",
      "description": "The Base44 backend server URL the app connects to (e.g. https://api.base44.com or your custom Base44 backend). The SDK calls `${serverUrl}/api` directly.",
      "requiredAtBoot": false
    }
  ]
}
